# Secrets (sensitive values)
# Copy to .secrets.env for local development.

# PostgreSQL connection string for Prisma.
DATABASE_URL=postgresql://postgres:password@db:5432/app

# JWT signing secrets — generate with: openssl rand -base64 32
AUTH_JWT_SECRET=change-me-access-secret
AUTH_JWT_REFRESH_SECRET=change-me-refresh-secret

# Database password for ephemeral RDS (Terraform).
TF_VAR_database_password=change-me-database-password
